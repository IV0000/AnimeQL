query Page($perPage: Int, $sort: [MediaSort]) {
  Page(perPage: $perPage) {
    media(sort: $sort) {
      averageScore
      coverImage {
        extraLarge
      }
      description
      episodes
      genres
      title {
        romaji
        english
      }
      duration
      format
      type
    }
  }
}
